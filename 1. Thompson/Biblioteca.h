#include <string> 
#include<bits/stdc++.h>
using namespace std;
namespace N{
    class Biblioteca{
    public:
    	int preced(char caracter);
    	string postfija(string exp);
    	string Agregando_simbolos(int longt, string ex);
   };
}
